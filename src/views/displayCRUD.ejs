<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>get CRUD !!! </title>
</head>
<style>
    table,
    th,
    td {
        border: 1px solid black;
        width: 300px;
    }
</style>

<body>

    <h2>TH elements define table headers</h2>

    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>password</th>
            <th>firstName</th>
            <th>lastName</th>
            <th>Address</th>
            <th>gender</th>
            <th>phoneNumber</th>
            <th>Action</th>
        </tr>
        <% for(let i=0; i<dataTable.length; i++) {%>
            <tr>
                <td>
                    <%= dataTable[i].email %>
                </td>
                <td>
                    <%= dataTable[i].password %>
                </td>
                <td>
                    <%= dataTable[i].firstName %>
                </td>
                <td>
                    <%= dataTable[i].lastName %>
                </td>
                <td>
                    <%= dataTable[i].address %>
                </td>
                <td>
                    <%= dataTable[i].gender %>
                </td>
                <td>
                    <%= dataTable[i].phoneNumber %>
                </td>
                <td>
                    <a type="button" class="" href="/gedit-crud?id=<%= dataTable[i].id %>">Edit</a>
                    <a type="button" class="" href="">Delete</a>
                </td>
            </tr>
            <% } %>
    </table>

    <p>To understand the example better, we have added borders to the table.</p>

</body>

</html>